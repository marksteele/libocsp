A small library to perform OCSP lookups.

Building it requires gnutls,gnutls-devel,libcurl,libcurl-devel,autoconf,libtool,automake

Building:

autoreconf --install
libtoolize
automake --add-missing
./configure
make
